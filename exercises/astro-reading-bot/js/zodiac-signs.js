//ARIES: March 21 – April 19

If (monthOfBirth == `march` && dayOfBirth > 20) ||(monthOfBirth == `april` && dayOfBirth < 20) {
    zodiacSign == `aries`
    }
     
    //TAURUS: April 20 – May 20
    If (monthOfBirth == `april` && dayOfBirth > 19 ) ||(monthOfBirth == `may` && dayOfBirth < 21 ) {
    zodiacSign == `taurus`
    }
    
    //GEMINI: May 21 – June 20
    If (monthOfBirth == `may` && dayOfBirth > 20 ) ||(monthOfBirth == `june` && dayOfBirth < 21 ) {
    zodiacSign == `gemini`
    }
    
    //CANCER: June 21 – July 22
    If (monthOfBirth == `june` && dayOfBirth > ) ||(monthOfBirth == `july` && dayOfBirth < ) {
    zodiacSign == `cancer`
    }
    
    //LEO: July 23 – August 22
    If (monthOfBirth == `july` && dayOfBirth > ) ||(monthOfBirth == `august` && dayOfBirth < ) {
    zodiacSign == `leo`
    }
    
    //VIRGO: August 23 – September 22
    If (monthOfBirth == `august` && dayOfBirth > ) ||(monthOfBirth == `september` && dayOfBirth < ) {
    zodiacSign == `virgo`
    }
    
    //LIBRA: September 23 – October 22
    If (monthOfBirth == `september` && dayOfBirth > ) ||(monthOfBirth == `october` && dayOfBirth < ) {
    zodiacSign == `libra`
    }
    
    //SCORPIO: October 23 – November 22
    If (monthOfBirth == `october` && dayOfBirth > ) ||(monthOfBirth == `november` && dayOfBirth < ) {
    zodiacSign == `scorpio`
    }
    
    //SAGITTARIUS: November 23 – December 21
    If (monthOfBirth == `november` && dayOfBirth > ) ||(monthOfBirth == `december` && dayOfBirth < ) {
    zodiacSign == `sagittarius`
    }
    
    //CAPRICORN: December 22 – January 19
    If (monthOfBirth == `december` && dayOfBirth > ) ||(monthOfBirth == `january` && dayOfBirth < ) {
    zodiacSign == `capricorn`
    }
    
    //AQUARIUS: January 20 – February 18
    If (monthOfBirth == `january` && dayOfBirth > ) ||(monthOfBirth == `february` && dayOfBirth < ) {
    zodiacSign == `aquarius`
    }
    
    //PISCES: February 19 – March 20
    If (monthOfBirth == `february` && dayOfBirth > ) ||(monthOfBirth == `march` && dayOfBirth < ) {
    zodiacSign == `pisces`
    }